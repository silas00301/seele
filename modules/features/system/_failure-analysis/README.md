# Failure analysis integration assets

The native reporter, rebuild wrapper, systemd generator and focused tests live in
[`seele-shell/projects/failure-analysis`](../../../../seele-shell/projects/failure-analysis/README.md).
The parent owns `failure-analysis.nix` service and executable-path wiring and the
existing Neovim scratch viewer in `view.lua`.
